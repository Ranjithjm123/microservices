version: "3.9"

services:
  user-app:
    build: ./User-app
    container_name: user-app
    ports:
      - "8081:8080"
#    networks:
#      - microservices-net

  post-it-app:
    build: ./Post-it-app
    container_name: post-it-app
    ports:
      - "8080:8080"
#    networks:
#      - microservices-net

#networks:
#  microservices-net:
#    driver: bridge
